export interface ADL {
    Key: string;
    Title: string;
    Value: number; // Used only within Apex
    ValueString: string; // Used only within Apex
    Hide?: boolean; // Used only within Apex
}
